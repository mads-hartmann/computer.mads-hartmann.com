version: "3.8"
services:
  jekyll:
    image: jekyll:local
    build: ./
    command:
      [
        "bundle",
        "exec",
        "jekyll",
        "serve",
        "--watch",
        "--drafts",
        "--source",
        "/usr/src/app/computer.mads-hartmann.com",
      ]
    ports:
      - 4000:4000
    volumes:
      - "./computer.mads-hartmann.com:/usr/src/app/computer.mads-hartmann.com"
